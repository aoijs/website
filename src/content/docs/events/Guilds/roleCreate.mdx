---
title: onRoleCreate
description: This event triggers whenever a guild role is created.
id: roleCreate
---

import Example from "../../../../components/Page/Example.astro";
import { Aside } from '@astrojs/starlight/components';

<Aside title="Intents">

This requires the `Guilds` intent!

</Aside>

<Aside title="Functions" type="tip">

This event has a function to retrieve data:

[$newRole[option]](/functions/newrole)

[$oldRole[option]](/functions/oldrole)

</Aside>

<Example>

<div slot="default">

```js
client.roleCreateCommand({
    name: "Role Create",
    channel: "$channelId",
    code: `
        $log[$newRole[id] was just created!]
    `
});
```

</div>
<div slot="handler">

```js
module.exports = {
    name: "Role Create",
    channel: "$channelId",
    type: "roleCreate",
    code: `
        $log[$newRole[id] was just created!]
    `
};
```

</div>
</Example>